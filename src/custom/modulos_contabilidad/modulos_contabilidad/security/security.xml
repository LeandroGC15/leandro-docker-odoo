<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Categoría de reglas de descuento -->
        <record id="module_category_account_discount" model="ir.module.category">
            <field name="name">Reglas de Descuento</field>
            <field name="description">Permisos para gestionar reglas de descuento por tipo de cliente</field>
            <field name="sequence">20</field>
        </record>

        <!-- Grupo para usuarios que pueden gestionar reglas de descuento -->
        <record id="group_account_discount_user" model="res.groups">
            <field name="name">Usuario / Reglas de Descuento</field>
            <field name="category_id" ref="module_category_account_discount"/>
            <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>
